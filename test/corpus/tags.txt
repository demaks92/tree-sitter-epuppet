==================
Expression tag inline
==================
prefix <%= $value %> suffix
---

(template
  (content)
  (output_directive
    (code
      (variable)))
  (content))


==================
Code block tag
==================
<% if $cond { %>
yes
<% } %>
---

(template
  (directive
    (code
      (identifier)
      (variable)))
  (content)
  (directive
    (code)))


==================
Code block with trim markers
==================
before
<%- $arr.each |$x| { -%>
  item: <%= $x %>
<%- } -%>
after
---

(template
  (content)
  (directive
    (code
      (variable)
      (identifier)
      (operator)
      (variable)
      (operator)))
  (content)
  (output_directive
    (code
      (variable)))
  (content)
  (directive
    (code))
  (content))


==================
Empty code tag
==================
<%  %>
---

(template
  (directive
    (code)))


==================
Expression with lambda using pipes
==================
<%= $list.map |$x| { $x * 2 } %>
---

(template
  (output_directive
    (code
      (variable)
      (identifier)
      (operator)
      (variable)
      (operator)
      (variable)
      (operator)
      (number))))
