==================
Output directive with percent close inside single string
==================
<%= notice('hello %> world') %>
---

(template
  (output_directive
    (code
      (identifier)
      (string))))


==================
Directive with percent close inside line comment
==================
<% # comment with %> inside
$x %>
---

(template
  (directive
    (code
      (comment)
      (variable))))


==================
Empty parameter directive
==================
<%- || -%>
---

(template
  (parameter_directive))


==================
Output directive with percent close inside interpolated double string
==================
<%= "value=${x}, %>" %>
---

(template
  (output_directive
    (code
      (string))))
